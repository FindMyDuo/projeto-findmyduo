import styled from "styled-components";

export const StyledDiv = styled.div`
  display: flex;
  flex-direction: column;
  gap: 32px;

  p {
    margin-bottom: -20px;
    margin-right: auto;
    color: var(--gray1);
  }
  button {
    margin-top: 16px;
  }
`;
